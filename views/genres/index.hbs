<div class="content">
    <h1>Liste des genres</h1>

    <ul class="list">
        {{#each genre}}
            <li class="item">
                <a href="/genres/{{id}}" class="link">{{name}}</a> 
            </li>
        {{/each}}
    </ul>
</div>